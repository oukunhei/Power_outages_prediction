# 气候指标计算方法恢复记录

## 数据来源与结构

三份气候 NetCDF 来自 Copernicus Climate Change Service / Climate Data Store，而不是 IRENA。IRENA属于能源数据来源。

| 指标 | 文件 | NetCDF变量 | 单位 |
|---|---|---|---|
| 年度最长连续干日 | `annual_consecutive_dry_days/cdd_CMIP5_rcp45_yr_2006-2100.nc` | `cdd` | day |
| 年度制冷度日 | `annual_cooling_degree_days/cd_CMIP5_rcp45_yr_2006-2100.nc` | `cd` | degC day |
| 年度采暖度日 | `annual_heating_degree_days/hd_CMIP5_rcp45_yr_2006-2100.nc` | `hd` | degC day |

每份文件覆盖2006–2100共95个年度、28个CMIP5模式成员以及90×180的2°全球网格。发布前会校验变量、单位、时间轴、成员数和CDS provenance。

## 恢复后的国家级计算步骤

对每一个指标和目标年份执行：

1. 选择该年份的28个模式成员网格；
2. 使用联合国国家边界建立国家—网格掩膜；
3. 使用 `cos(latitude)` 作为网格面积权重；
4. 对每个模式成员分别计算国家内有效网格的面积加权平均；
5. 对一个国家的28个模式结果取中位数，作为正式指标；
6. 同时保存模式间P10、P90和有效成员数；
7. 对2°网格无法覆盖的小岛或小区域，使用该区域代表点最近的网格，并记录 `climate_spatial_fallback=true`；
8. 使用严格大于号生成风险标记：连续干日 > 30、CDD > 300、HDD > 3000。

实现位于 `src/outage_prediction/processing/climate.py`。多年份只构建一次空间掩膜，并仅遍历每个国家实际包含的网格，以降低2030–2100批量计算成本。

## 与2024年遗留表的恢复验证

遗留的 `dry_country.csv`、`cdd_country.csv`、`hdd_country.csv` 没有年份和计算代码。使用现有NetCDF的2100年值按上述方法重建后，在178个可匹配国家上的新旧排序相关性为：

| 指标 | Pearson | Spearman | 平均绝对差 |
|---|---:|---:|---:|
| dry | 0.971756 | 0.972239 | 6.224286 day |
| CDD | 0.973815 | 0.979936 | 123.399098 degC day |
| HDD | 0.962940 | 0.993106 | 142.753503 degC day |

在2030–2100逐年对照中，dry和CDD均在2100取得最小平均绝对差，HDD在2099最小且与2100非常接近。这支持遗留表来自世纪末数据的判断。

旧值无法逐项精确复现，差异主要集中在澳大利亚、加拿大、俄罗斯、阿尔及利亚等大国，说明遗失步骤可能使用了另一版国家边界、栅格覆盖规则或空间权重。额外测试的国家内不加权平均、先取模式网格中位数、国家外接矩形平均均未比当前方法更吻合。因此正式版本采用上面可解释、可测试且面积口径正确的方法，不继续模仿无法确认的旧GIS操作。

## 发布年份和解释边界

- 前端可选择2030–2100的任意整数年份；
- 2030、2050、2100另外生成使用相同色标的静态地图；
- 所有年份暂时共用GIPT August 2026 v3在运设施能源结构；
- 因此年份间变化只来自气候输入，结果应解释为“固定能源结构下的气候情景压力测试”；
- `risk_score` 是相对风险得分，不是停电概率；
- 旧Excel的HDD > 300错误标记不再复现或发布。
